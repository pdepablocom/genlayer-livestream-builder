// Measurements shared by every template, in artboard pixels (2880×1620).
// Each template keeps its own layout table in js/templates/.

const ARTBOARD = { w: 2880, h: 1620, margin: 96 };

const MARK = { x: 2626, y: 1376 };
const TITLE_STEPS = [160, 144, 128, 112, 96];
const COLUMN = { x: 96, y: 96, h: 1428 };
